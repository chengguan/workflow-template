# Archive

Frozen handoff logs and old `AGENT-NOTES.md`. Never auto-loaded.

`git mv` closed `docs/tasks/<id>.log.md` here (or leave it and drop it from `NOW.md`). Do not merge archive back into `NOW.md`.
