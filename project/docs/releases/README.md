# Releases

One file per shipped version: `{{VERSION}}.md`.

What shipped, what was deferred, residuals still open. Pointer from `docs/DECISIONS.md` if a release encoded a decision. Update the shipping line in `AGENTS.md` in the same change.
